// In your new script's .gs file
function useLibraryDemo() {
// Suppose the library's identifier is "LibraryDemo"
const message = LibraryDemo.greet("Alice");
Logger.log(message); // "Hello, Alice! From LibraryDemo."
}